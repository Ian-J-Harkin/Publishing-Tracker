// test_package.json (TypeScript content for demonstration purposes)

import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RequestPlatformComponent } from 'src/app/features/platforms/request-platform/request-platform.component';
import { of } from 'rxjs';

// Mock service
class MockPlatformService {
  requestPlatform = jest.fn().mockReturnValue(of(null));
}

describe('RequestPlatformComponent', () => {
  let component: RequestPlatformComponent;
  let fixture: ComponentFixture<RequestPlatformComponent>;
  let platformService: MockPlatformService;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [RequestPlatformComponent],
      providers: [
        { provide: 'PlatformService', useClass: MockPlatformService }
      ]
    }).compileComponents();

    fixture = TestBed.createComponent(RequestPlatformComponent);
    component = fixture.componentInstance;
    platformService = TestBed.inject('PlatformService') as unknown as MockPlatformService;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });

  it('should call requestPlatform and return observable', () => {
    const spy = platformService.requestPlatform;
    component.requestPlatform();
    expect(spy).toHaveBeenCalled();
  });
});