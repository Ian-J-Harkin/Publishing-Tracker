// src/styles/theme.ts
export const theme = {
    layout: {
        sidebarWidth: '260px',
        headerHeight: '64px',
    },
    // We can still use these in inline styles if needed
    colors: {
        danger: 'var(--danger)',
        textMuted: 'var(--text-muted)',
        border: 'var(--border)',
    }
};